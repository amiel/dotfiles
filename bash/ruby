
if [[ -s /Users/amiel/.rvm/scripts/rvm ]] ; then source /Users/amiel/.rvm/scripts/rvm ; fi

# Enable pow to use .com domains that are in the /etc/hosts file.
export POW_EXT_DOMAINS=com

# Use bundle binstubs first. Make sure this goes after rvm initialization
export PATH="./bin:$PATH"