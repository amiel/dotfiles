h2. Dotfiles!

These are my dotfiles.

I clone this to Dropbox at ~/Dropbox/dotfiles but it should work anywhere

Then run bin/link-dotfiles

h2. Sensitive Data

Sensitive data is stored in ~/.env which is sourced by .bashrc.
