h2. Dotfiles!

These are my dotfiles.

I clone this to Dropbox at ~/Dropbox/dotfiles but it should work anywhere

Then run bin/link-dotfiles

h2. Sensitive Data

Sensitive data is stored in ~/.env which is sourced by .bashrc.

h3. Instructions

1. Open "Keychain Access"
2. Click on the + to create a new key
3. Use an identifier from the list below for the "Name" field
4. Enter the password/token in the "Password" field

h3. Identifiers

* Github API Token