#!/bin/bash

if [[ -z "$1" ]]; then
  echo "$0: argument required"
  exit 1
fi

setting_name=$1
value=$(eval echo \$"$setting_name")

if [[ -z "$value" ]] && $IS_OSX;then
  # If we have no value, attempt loading the value from the os x keychain
  read-password $*
else
  echo $value
fi
