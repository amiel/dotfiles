#!/bin/bash

function set_color() {
  local rbenv_version_save="$RBENV_VERSION"
  export RBENV_VERSION=2.1.0
  digicolor $1
  export RBENV_VERSION="$rbenv_version_save"
}

set_color black
if eval "$@";then
  set_color blue
else
  set_color '#ff0009'
fi

