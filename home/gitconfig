[user]
  name = GIT_NAME
  email = GIT_EMAIL

[github]
  user = GITHUB_USER
  token = GITHUB_TOKEN

[filter "securegitconfig"]
  smudge = git-filter-insert-sensitive-information
  clean = git-filter-clean-sensitive-information

[diff "exif"]
  # brew install exiftool
  textconv = exiftool


[core]
  # editor         = mate -w
  # I'd rather edit my commit messages in vim.
  # Use the above for textmate
  editor           = vim
  filemode         = true
  logallrefupdates = true
  whitespace       = fix, space-before-tab, trailing-space

[merge]
  tool = opendiff

[color]
  branch    = auto
  diff      = auto
  status    = auto
[color "branch"]
  current   = yellow reverse
  local     = yellow
  remote    = green
[color "diff"]
  meta      = yellow bold
  frag      = magenta bold
  old       = red bold
  new       = green bold
[color "status"]
  added     = yellow
  changed   = green
  untracked = cyan

[color]
  ui = true

[alias]
  st      = status
  s       = status -sb
  ci      = commit
  br      = branch
  brav    = branch -a -v
  brv     = branch -v
  co      = checkout
  lg      = log -p
  ll      = log --pretty=oneline --abbrev-commit --max-count=15
  browse  = !hub browse
  w       = !hub browse
  compare = !hub compare

[branch]
  autosetupmerge = true

[push]
  default = tracking
